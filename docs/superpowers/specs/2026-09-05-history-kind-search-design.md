# 历史账目类型搜索设计

## 目标

现有历史搜索框支持按当前界面语言搜索收入和支出，同时保留日期、备注、分类搜索。

## 行为

- 简体、繁体和英文分别使用当前语言的 `entry.income`、`entry.expense` 文案参与搜索。
- 搜索继续使用现有的本地化、不区分大小写包含匹配，因此英文部分输入也有效。
- 只匹配当前界面语言，不新增跨语言别名、筛选按钮或搜索语法。

## 实现与验证

`HistorySearchIndex` 接收收入和支出的显示名称；`HistoryView` 从现有本地化资源传入。测试覆盖三种语言的收入/支出，并确认原有搜索不回归。
