# 进度追踪

## 🎯 当前目标
按已批准规格实施 macOS 自用开源记账应用“罗密欧每日记账 / Romeo Daily Ledger”；采用多个独立对话按编号任务串行实现，下一步为计划 1 的任务 1。

## ✅ 完成进度
- [x] 检查项目上下文并连接目标 GitHub 仓库
- [x] 通过头脑风暴确认产品目标、约束与视觉方向
- [x] 对比方案并完成设计审批
- [x] 编写并自检产品设计规格
- [x] 用户批准产品定位与完整设计规格
- [x] 制定核心账本与原生 UI 实施计划
- [x] 制定数据管理与 AI 助手实施计划
- [x] 制定手动更新、开源与发布实施计划
- [x] 完成规格覆盖、占位符、路径和测试辅助类型审计
- [x] 选择执行方式：每个独立对话实现一个编号任务
- [ ] 在新对话中执行计划 1、任务 1

## 📋 关键决策
- 默认面向 macOS、个人自用优先，同时以适合 GitHub 开源的方式组织项目。
- 在设计获批前不搭建项目或编写实现代码。
- 用户已批准在视觉问题上使用浏览器视觉伴侣进行原型和方案对比。
- 首版采用完全本地优先：无需账号、离线可用，并支持数据导入、导出和备份；暂不做云同步。
- 首版账目类型为支出与收入，不包含账户间转账。
- 首版不设置账户与余额体系；账目作为独立流水记录。
- 收支使用独立分类：支出为衣、食、住、行、娱乐、其他；收入为工资、奖金、理财、退款、其他；未选择时归入对应“其他”。
- 首页核心任务为查看今日流水并快速新增账目；日历作为独立入口。
- 首版提供基础统计：本月收入、支出、结余、分类占比和简单趋势；不含预算、同比环比与智能洞察。
- 默认币种为美元（USD），可在 General 设置中更改；单个账本只使用一种币种，不做汇率换算。
- 交互采用鼠标与键盘并重：支持 Tab、回车提交与常用快捷键；首版不做跨应用全局唤起。
- 产品路线采用“安静的每日账本”：首页强调今日流水与快速录入，独立日历负责时间回看，基础统计保持克制。
- 双主题视觉已获确认：浅色继承 A 的温暖编辑感，深色继承 C 的石墨工作室感；两者共用组件、布局和信息层级。
- 技术路线采用原生 SwiftUI + SwiftData，最低支持 macOS 14。
- 信息架构已确认：一级入口为记账、日历、统计、设置；设置支持 `⌘,`，内部分为通用与外观；选定语言后系统文案不混用中英文。
- 分类采用“内置 + 自定义”：支持新增、重命名、排序和隐藏；内置“其他”不可删除，隐藏分类继续保留历史账目。
- 数据规则已确认：账目使用十进制金额，未分类自动归入“其他”；混合多选显示收入、支出和净额；删除支持确认与撤销；恢复备份前创建快照。
- 新增 AI 记账需求：用户配置 API Key，以自然语言描述金额和用途，AI 生成可修改账目；所有账目均支持后期编辑。
- 导入、导出、备份与恢复明确放在“设置 → 通用 → 数据”。
- AI 不按厂商选择，而按模型协议配置；用户为连接填写 Base URL、API Key 和模型 ID，以覆盖主流及兼容厂商。此前“六家提供方适配器”决策作废。
- “协议”指具体 API 规范，例如 OpenAI Chat Completions、OpenAI Responses、Anthropic Messages、Gemini GenerateContent，而不是厂商预设。
- 界面与规格不列出模型厂家名称或厂家按钮；只显示协议、连接名称、Base URL、API 密钥和模型 ID。
- AI 解析后只生成可编辑草稿，必须由用户确认才写入账本；保存后的账目仍可继续编辑。
- AI 首版仅提供文字输入，不申请麦克风权限；用户可使用 macOS 系统听写。
- AI 支持一句话解析多条账目，生成多张可编辑草稿后统一确认保存。
- AI 还需支持账目问答与分析：按用户要求读取本地收入/支出记录并生成分析；先前“不发送历史账目”的拟议边界未获批准。
- AI 分析选择允许发送完整账本；仅在用户主动发起分析且开启授权后发送，默认授权关闭，大账本需分批处理且不得截断。
- 完整账本分析包含全部备注，首次开启必须明确披露发送字段、提供方、费用和远端隐私影响。
- AI 采用独立一级页面，内分“AI 记账”和“账目分析”；记账页同时保留 AI 快捷入口。AI 设置中的六家厂商按钮方案未获批准。
- 最终 AI 架构已确认：协议为 Chat Completions、Responses、Messages、GenerateContent；支持多连接、钥匙串密钥、完整账本与备注分析；侧边栏不显示快捷键提示。
- 错误处理与测试策略已确认：AI 故障不影响手动功能，导入先预览，恢复先快照，协议使用模拟服务测试，真实 API 测试手动启用。
- 项目采用 MIT 开源许可证。
- 正式规格文件位于 `docs/superpowers/specs/2026-08-29-romeo-daily-ledger-design.md`，目标仓库为 `https://github.com/DUGUSHUANGTAN/Romeo-Daily-Ledger`。
- 软件更新改为完全手动：设置中的通用分栏显示当前版本和“检查更新”按钮；启动及后台不访问更新源，点击后才读取 GitHub Pages 更新清单，用户确认后安装 GitHub Releases 更新包。
- 实施拆成三份顺序计划：`docs/superpowers/plans/2026-08-29-core-ledger-and-ui.md`、`2026-08-29-data-and-ai-assistant.md`、`2026-08-29-updates-open-source-release.md`；总路线图为 `2026-08-29-romeo-daily-ledger-roadmap.md`。
- 所有计划采用测试驱动的小步提交；UI 与品牌资产开始前必须读取并应用 Taste Skill，交付图标限定为固定版本 Lucide SVG。
- 未经再次授权，不执行 `git push`、创建 GitHub Release、配置 Pages 或写入仓库 Secrets。
- 实现采用独立对话串行推进：每个对话只完成一项编号任务、运行定向与相关回归测试并提交；完成并确认工作区干净后，再开始下一项，禁止并行修改同一仓库。

## 🔄 上下文恢复信息
产品规格和三阶段实施计划均已完成。用户决定在其他独立对话中逐项实现。下一个对话从 `docs/superpowers/plans/2026-08-29-core-ledger-and-ui.md` 的“任务 1：建立可生成、可测试的 macOS 工程”开始；开始前检查 `git status --short` 和最近提交，结束时更新本文件。
